generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = "postgres://postgres:postgres@localhost:5432/postgres"
}

model repro {
  id String @id @db.Uuid
}
